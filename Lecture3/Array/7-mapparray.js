const finalParticpants = ["Taylor" , "Donald" , "Don" , "Natasha" , "Bobby"];

const annouuncements = finalParticpants.map((member) => {
    return member + " Joined the cotest. " + "\n";
});

console.log(annouuncements);
//slide13